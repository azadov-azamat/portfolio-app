import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["it-market.uz", "static8.tgstat.ru", "huquqiyportal.uz", "encrypted-tbn0.gstatic.com"],
  },
};

export default nextConfig;
